---
import './PostTags.css';

const { tags } = Astro.props;
---

<div class="post-tags">
  {
    tags.map((tag) => {
      <a class="post-tags__link" key={tag.id} href={tag.path}>
        <span>#</span> {tag.title}
      </a>;
    })
  }
</div>
